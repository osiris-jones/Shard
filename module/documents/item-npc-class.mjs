/**
 * item-npc-class.mjs — NPC Class Item document class.
 */

import { ShardItem } from "./item.mjs";

export class ShardNPCClassItem extends ShardItem {
  // NPC classes don't have rank-eligibility logic; the GM assigns them freely.
}
