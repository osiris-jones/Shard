/**
 * item-npc-template.mjs — NPC Template Item document class.
 */

import { ShardItem } from "./item.mjs";

export class ShardNPCTemplateItem extends ShardItem {
  // NPC templates layer abilities onto an NPC without providing base stats.
}
